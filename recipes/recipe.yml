---
# yaml-language-server: $schema=https://schema.blue-build.org/recipe-v1.json

name: bluefin-gdx-workstation
description: Custom Bluefin GDX with 1Password

# GDX LTS base image - CentOS Stream 10 with NVIDIA + CUDA
base-image: ghcr.io/ublue-os/bluefin-gdx
image-version: lts

modules:
  # 1Password repo + packages
  - type: script
    snippets:
      - |
        # Add 1Password RPM repository
        rpm --import https://downloads.1password.com/linux/keys/1password.asc
        cat > /etc/yum.repos.d/1password.repo << 'EOF'
        [1password]
        name=1Password Stable Channel
        baseurl=https://downloads.1password.com/linux/rpm/stable/$basearch
        enabled=1
        gpgcheck=1
        repo_gpgcheck=1
        gpgkey=https://downloads.1password.com/linux/keys/1password.asc
        EOF
        
        # Install 1Password GUI + CLI
        dnf install -y 1password 1password-cli
        
        # Clean up dnf cache to keep image small
        dnf clean all

  - type: signing
